"""
1) Add the lesson and activity details.
   a) Mention the lesson as "Introduction to Matplotlib".
   b) Mention the activity name as "My Score Tracker".
   c) Mention the file name.

2) Import Matplotlib.
   a) Import `matplotlib.pyplot` as `plt`.
   b) Use `plt` to create charts.

3) Create the chart data.
   a) Store school days in the `days` list.
   b) Store quiz scores in the `scores` list.
   c) Match each score with its day.

4) Draw the first line graph.
   a) Use `plt.plot(days, scores)`.
   b) Use `plt.show()` to display the chart.

5) Add chart labels and title.
   a) Use `plt.title()` to add a chart heading.
   b) Use `plt.xlabel()` and `plt.ylabel()` to label both axes.
   c) Use `plt.grid(True)` to add gridlines.
   d) Use `plt.ylim(0, 100)` to set the score range.

6) Style the line chart.
   a) Set the line colour.
   b) Add circle markers for each score.
   c) Use a dashed line style.
   d) Increase the line width.

7) Draw a bar chart.
   a) Use `plt.bar(days, scores)` to create bars.
   b) Set the bar colour to orange.
   c) Add a title and axis labels.
   d) Set the y-axis range from 0 to 100.
   e) Use `plt.show()` to display the chart.
"""